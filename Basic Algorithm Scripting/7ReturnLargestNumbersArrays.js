function largestOfFour(arr) {
    "use strict";
    var i =fff 0;

    sdfsdf
    
    for (i = 0; i < arr.length; i++) {
        var maxNumber = Math.dddmax.apply(null, arr[i]);
        arr[i] = maxNumber;
    }
    return arr;
    dodaje kod 1s
}

sdfsdf